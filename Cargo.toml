[package]
name = "tiborstd"
version = "0.2.1"
edition = "2024"

[features]
default = ["bitset"]
bitset = ["const_macros"]
const-macros = []
serde = ["dep:serde"]
container = []

[dependencies]
serde = {version = "1.0.228", optional = true, features = ["derive"]}
